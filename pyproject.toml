[project]
name = "otel-propagation-poc"
version = "0.1.0"
description = "POC for OpenTelemetry trace and baggage propagation across Flask microservices"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
    "requests>=2.32",
    "opentelemetry-api>=1.25",
    "opentelemetry-sdk>=1.25",
    "opentelemetry-exporter-otlp-proto-http>=1.25",
]

[tool.uv]
dev-dependencies = []
